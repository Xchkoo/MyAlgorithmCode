#include<iostream>
using namespace std;
#define MAXN 1000

int main(){
    int n,v;
    cin >> n >> v;
    int weight[MAXN], value[MAXN];
    for(int i=0;i<n;i++){
        cin >> weight[i];
        cin >> value[i];
        }
    
    return 0;
}